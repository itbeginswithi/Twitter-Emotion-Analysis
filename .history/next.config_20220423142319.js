/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["pbs.twimg.com"],images: {
      domains: ['assets.example.com'],
    },
  },
  reactStrictMode: true,
}

module.exports = nextConfig
